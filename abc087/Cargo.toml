[package]
name = "abc087"
version = "0.1.0"
edition = "2021"

# dependencies added to new project
# competitive = { git = "https://github.com/tanakh/competitive-rs.git" }
[dependencies]
proconio = "0.4.5"

[profile.release]
lto = true
panic = 'abort'
